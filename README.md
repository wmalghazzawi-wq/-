# يقدم هذا المشروع منصة تعليمية وتحليلية متكاملة تضم: شرح علمي مبسّط لمفاهيم المواد النانوية، واجهة تفاعلية تعليمية، نظام تحليل ذكي قائم على الذكاء الاصطناعي لتفسير بيانات حيود
الأشعة السينية (XRD). حيث تم تطوير النظام لتصنيف أنواع أكاسيد المعادن تلقائيًا من صور أنماط XRD باستخدام نموذج تعلم عميق يعتمد على Transfer Learning.
هدف المشروع إلى:
تقليل الاعتماد على المكتبات التجارية المكلفة
تسريع عملية تحليل المواد
ربط الجانب التعليمي بالتطبيق العملي الواقعي

كيف يعمل المشروع؟

# CNN Project

## Description

This project implements a Convolutional Neural Network (CNN) for image classification.

## Requirements

The project requires Python and the following libraries:

* numpy
* torch
* torchvision
* opencv-python

All dependencies are listed in `requirements.txt`.

## Installation

1. Clone the repository:

```
git clone https://github.com/USERNAME/REPOSITORY.git
```

2. Move to the project folder:

```
cd REPOSITORY
```

3. Create a virtual environment:

```
python -m venv cnn_env
```

4. Activate the environment:

Linux / macOS:

```
source cnn_env/bin/activate
```

Windows:

```
cnn_env\Scripts\activate
```

5. Install dependencies:

```
pip install -r requirements.txt
```

## Running the Project

Run the main file:

```
python main.py
```

## Project Structure

```
project/
│
├── data/
├── models/
├── main.py
├── requirements.txt
└── README.md
```

